<% layout('layouts/boilerplate')%> <!--all content passed to boilerplate template as body (bc it was orig the body)-->
    <h1>All Campgrounds</h1>
    <div>
        <a href="/campgrounds/new">Add New Campground</a>
    </div>
    <ul>
        <% for (let campground of campgrounds) { %>
           <!-- <li><a href="/campgrounds/<%= campground._id %>"><%= campground.title %></a></li> <!--reference campground.js w/ link leading to /campgrounds/:id
        <% } %>
    </ul>  --> 
        <div class="card" mb-3>
            <div class="col-md-4"> <!--w/ after >size medium, want to take up 4/12 units (1/3 of row)-->
                <img class="img-fluid" alt="" src="<%= campground.image %>">
            </div> 
        </div>
    

